//
// Created by nolasco on 14/05/20.
//

#define CATCH_CONFIG_MAIN
#include <catch.hpp>

SCENARIO("Clamped addition", "[MathUtils]") {

  GIVEN("An example setup") {
    WHEN("some statement takes place") {
      THEN("all according to plan") {
        REQUIRE(true);
      }
    }
  }
}
