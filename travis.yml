language: cpp
os: linux
dist: focal
compiler:
  - gcc
  - clang

script:
  - mkdir build
  - cd build
  - cmake -DCMAKE_BUILD_TYPE=Debug ../
  - cmake -DCOMPILE_FOR_NON_UNIX:BOOL=OFF ../..
  - make test
  - make text-game
